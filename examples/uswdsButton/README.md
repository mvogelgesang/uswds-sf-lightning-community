# USWDS Button

A quality example of embedding buttons into wrapper components can be found in the `testButtonWrapper` component which is included in the `/test/` directory.

The `testButtonWrapper` component provides a full working example of event propogation from the base uswdsButton component up to the wrapper and then back down again. The component can be placed on any Experience Builder page.
